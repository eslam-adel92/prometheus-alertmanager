# Loki Datasource — Auto-provisioned
# Provides log querying with LogQL alongside Prometheus metrics.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: true
    jsonData:
      maxLines: 1000
